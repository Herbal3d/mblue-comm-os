# mblue.comm.os

`mblue.comm.os` is a shared .NET common library for MBlue projects.  
This provides the ICommService for connecting to OpenSimulator
and its compatible services.

Target framework: `net10.0`
